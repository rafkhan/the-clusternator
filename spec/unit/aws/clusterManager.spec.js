'use strict';

var rewire = require('rewire'),
  C = require('../chai');

var Cluster = rewire('../../../src/aws/clusterManager');


/*global describe, it, expect, beforeEach, afterEach */
/*eslint no-unused-expressions: 0*/
describe('clusterManager', () => {
});
