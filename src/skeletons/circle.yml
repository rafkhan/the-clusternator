machine:
  node:
    version: 4.2.3
  services:
    - docker

dependencies:
  post:
    - ./scripts/clusternator-decrypt.sh

test:
  post:
    - ./scripts/clusternator-docker-build.sh
    - ./scripts/clusternator-decrypt.sh
    - node ./scripts/clusternator-notify.js
