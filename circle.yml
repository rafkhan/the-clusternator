machine:
  node:
    version: 4.2.3
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get install libffi-dev libssl-dev
    - pip install pyopenssl ndg-httpsclient pyasn1
    - pip install awscli
